import 'package:flutter/material.dart';

class CategoryTwoView extends StatelessWidget {
  const CategoryTwoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
