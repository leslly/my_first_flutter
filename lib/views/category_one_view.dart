import 'package:flutter/material.dart';

class CategoryOneView extends StatelessWidget {
  const CategoryOneView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
