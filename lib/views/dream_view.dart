import 'package:flutter/material.dart';

class DreamView extends StatelessWidget {
  const DreamView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
