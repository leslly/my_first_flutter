import 'package:flutter/material.dart';

class CategoryThreeView extends StatelessWidget {
  const CategoryThreeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
